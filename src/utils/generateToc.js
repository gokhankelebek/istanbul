// Simple placeholder TOC generator for blog posts
// Accepts HTML string, returns an empty array (no TOC)
// Replace with real implementation as needed
export default function generateToc(html) {
  return [];
}
